game-data-packager is maintained in git. The git repository can be obtained at
  <git://git.debian.org/git/pkg-games/game-data-packager>

The following branches are used for the following purposes:

  origin/master - current package in sid
  origin/quake3 - implementing quake3 support
  origin/rott   - implementing rise of the triad (rott) support
